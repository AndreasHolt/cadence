apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cadence-matching-a
spec:
  template:
    spec:
      containers:
        - name: matching
          resources:
            requests:
              cpu: "1"
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 512Mi
