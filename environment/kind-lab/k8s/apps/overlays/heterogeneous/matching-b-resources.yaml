apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cadence-matching-b
spec:
  template:
    spec:
      containers:
        - name: matching
          resources:
            requests:
              cpu: "2"
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 512Mi
